defmodule Bonny.WatcherTest do
  use ExUnit.Case, async: true
  # alias Bonny.Watcher

  # setup do
  #   watcher = Watcher.new()
  #   %{watcher: watcher}
  # end

  # test "example", %{watcher: watcher} do
  #   assert Watcher.lookup(watcher, "hello") == :error

  #   Watcher.store(watcher, "hello", "world")
  #   assert {:ok, "world"} = Watcher.lookup(watcher, "hello")
  # end
end
